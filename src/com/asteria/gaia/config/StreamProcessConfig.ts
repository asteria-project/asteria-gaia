/**
 * The <code>StreamProcessConfig</code> interface defines the common functionality implemented by context configuration
 * objects applyed to Asteria stream processes.
 */
export interface StreamProcessConfig {
    [key: string]: any;
}
