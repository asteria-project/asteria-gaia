/**
 * The <code>AsteriaSessionConfig</code> interface defines the configuration of an Asteria application session.
 */
export interface AsteriaSessionConfig {

    /**
     * The name of the Asteria session associated with this config.
     */
    name: string;
}
