/**
 * The <code>AsteriaLine</code> interface represents a single line in an input string.
 */
export interface AsteriaLine {

    /**
     * The position of this line in the input string.
     */
    index: number;

    /**
     * The content of this line in the input string.
     */
    value: string;
}
