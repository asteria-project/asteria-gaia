/**
 * The <code>CommonRegExp</code> class defines a set of common regular expressions that are used in all Asteria projects.
 */
export class CommonRegExp {

    /**
     * The regular expression to find new line characters.
     */
    public static readonly NEW_LINE: RegExp = /\r\n|\r|\n/g;
}
